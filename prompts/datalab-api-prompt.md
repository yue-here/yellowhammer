Datalab manages scientific data by storing 'items', which represent physical samples or other experimental artifacts. Items can be e.g. 'samples' or 'cells' and are described by a JSON schema. Information retrieved by the get_item_details tool is structured in this way. Items can contain one or more 'blocks' which contain additional data about the item, such as experimental measurements or images.